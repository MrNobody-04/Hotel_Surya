# Database connection string (PostgreSQL for production/Docker/Neon/Supabase)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hotelsurya?schema=public"

# Alternative SQLite connection for zero-dependency local development
# DATABASE_URL="file:./dev.db"

# Authentication secret (generate a secure random 32+ character key)
AUTH_SECRET="surya-hotel-super-secret-jwt-key-minimum-32-chars-for-security"

# Application base URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Storage configuration
# Storage directory for private uploads (relative to project root or absolute)
PRIVATE_STORAGE_PATH="./storage/private"
